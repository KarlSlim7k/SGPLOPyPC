# Variables de entorno para SGPLOPyPC
# Copiar a .env y ajustar valores reales (nunca commitear .env)

# Base de datos
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sgplopypc
DB_USER=root
DB_PASS=

# Seguridad
JWT_SECRET=cambia_este_secreto_en_produccion_2026
JWT_TTL=86400

# Entorno
APP_ENV=development
